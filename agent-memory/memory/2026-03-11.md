# 2026-03-11

- Continued ops-dashboard work: restored HTTP on port 80, then upgraded dashboard to HTTP bootstrap + WebSocket updates on :8765 with bucket snapshots, bucket detail, candle snapshots, trade/event updates, and a lightweight SVG candlestick view.
- Fixed dashboard candle-loading bugs: first, bucket detail needed strategy account fallback from health.strategy_accounts; second, swap symbols needed `BTC/USDT:USDT`-style normalization; third, frontend `workspace_snapshot` was overwriting cached bucketDetails/candles. After fixes, BTC breakout bucket loaded 60+ candles correctly.
- Expanded Buckets page UX: refresh/status bar, summary strip, trade table metadata/empty state, selected-trade chart focus window, event markers, volume panel, MACD panel, time axis, price labels, and chart legend.
- Investigated recurring OKX trade halt: real mismatch was exchange residual position on pullback account (`openclaw2` internally), `ETH/USDT:USDT` short 283.32 contracts, while local state was flat (`missing_local_position`).
- User clarified operations preference: known/repeated old problems may be investigated and handled directly without asking again; genuinely new problems should still be escalated for user decision. Discord alerts should be deduplicated and sent as analysis reports, one notification per issue unless status meaningfully changes.
- Updated USER.md with that incident-handling preference.
- Modified okx-trading mismatch presentation so outward-facing analysis includes strategy/display account names (e.g. `pullback`) instead of only internal aliases like `openclaw2`.
- Added automatic alignment diagnosis output at `projects/crypto-trading/reports/changes/latest-alignment-diagnosis.json`, including mismatch classification, recommended action, and whether auto-release is allowed.
- Added deduplicated Discord-style alignment alert logic so the same mismatch fingerprint should only notify once, with follow-up only on change or resolution.
- User requested Codex auth/profile cleanup: verified only `default` and `backup` had real OAuth contents; `primary` existed only in config, not active auth storage.
- Swapped real OAuth contents between `openai-codex:default` and `openai-codex:backup`, restored auth priority to `default -> backup`, and removed stale `primary` from config. Result: `default` remains first, but now contains the old backup account’s auth content.
- Automatically handled the known residual-position problem: executed single-position reduce-only close for pullback ETH short 283.32 contracts, re-ran reconcile, verified `exchange_live_positions` became empty and `position_alignment.ok = true`, then sent a Discord resolution update.
- Rebuilt `projects/crypto-trading` away from the old OKX demo scaffold and made `src/v2/` the new mainline architecture; removed large legacy subsystems (old runner/strategy/review/risk/storage/execution/notify paths and related scripts/tests) while preserving `src/config/settings.py`, `src/exchange/okx_client.py`, and v2 code/tests.
- Standardized account/role naming around regime-oriented accounts: `trend`, `meanrev`, `compression`, `crowded`, `realtime`; `chaotic` remains a no-trade state, not an account.
- Established v2 design principle: one unified Market Data Hub with multiple strategy-specific views, not separate data pipelines and not one raw microstructure feed directly used by all strategies.
- Formalized regime vocabulary for v2 as: `trend`, `range`, `compression`, `crowded`, `shock`, `chaotic`, with routing `trend->trend`, `range->meanrev`, `compression->compression`, `crowded->crowded`, `shock->realtime`, `chaotic->no-trade`.
- Implemented v2 backbone files and tests: market hub, feature engine, rule-based classifier, layered classifier, BTC regime runner, shock monitor, OKX public websocket skeleton, and related tests.
- Implemented layered regime classification structure: 4h background layer, 15m primary classification layer, 1m shock override layer; main runner now outputs per-layer decisions and per-layer feature snapshots.
- Confirmed real OKX-derived crowded features now produce meaningful values on BTC: funding percentile and OI acceleration are live; basis deviation was fixed using mark/index fallback from mark/index OHLCV when funding endpoint lacks mark/index fields.
- Implemented derivatives history bootstrap on startup using OKX/ccxt funding history, open interest history, and mark/index OHLCV-derived basis history so crowded features do not start from an empty history window.
- Implemented real OKX public websocket ingestion skeleton for shock/event data using trades, bbo-tbt, and liquidation-orders channels; normalized events feed into the shared hub.
- Shock/event feature chain now supports trade burst score, liquidation spike score, orderbook imbalance, and event-layer feature extraction from real WS-fed data.
- Main regime runner now supports a shock-enhanced run mode that listens to OKX public websocket events for a short window before final layered classification.
- Current observed BTC outputs during development often showed 4h background = `trend`, 15m primary = `compression`, and no 1m shock override; this indicates the system is now distinguishing higher-timeframe trend background from lower-timeframe compression structure.
- Chentong clarified product direction: rebuild from scratch rather than patching the old system; focus on BTC only for now; use regime recognition to select the most appropriate strategy account instead of seeking a single universally best strategy; ML should prioritize market/regime recognition and confidence estimation over direct price prediction.
- Added a session-continuity workflow: maintain a concise handoff file for next-session resume, preserve full session archives, and prefer frequent GitHub commits/pushes after each completed task.
- Per user request, promoted the former `src/v2/` rebuild into the mainline codebase: moved the new architecture into stable top-level `src/*` packages, removed `v2` markers from package paths/scripts/tests/docs/log paths, kept `okx` naming only for exchange-specific adapters, and fully deleted legacy old-version code from the working tree.
- Fixed a real config-loading bug during the rename pass: `Settings.load()` now uses explicit env-file override so tests/loaders are not polluted by ambient shell variables.
- Verified the renamed crypto-trading mainline with a full green test run (`77 passed`) and pushed commit `47a0529` (`Promote v2 mainline to stable crypto packages`) to GitHub.
- Added a unified decision summary layer to the regime runner so each run now emits a stable action-oriented summary (`regime`, `confidence`, routed account, allow/block reason, diagnostics) instead of forcing downstream consumers to reconstruct those semantics from separate fields.
- Preserved backward compatibility for older test helpers/consumers by making the new runner summary field optional at construction time, then revalidated the whole repo with `81 passed`.
- Extended the execution pipeline to consume the runner's decision summary directly and produce an explicit `decision_trace` object, so trade execution now records mode gating, decision gating, route freeze, alignment-policy outcomes, and final allow/block reasons in one place.
- Added dedicated pipeline-trace tests and revalidated the whole repo with `83 passed`.
- Added stable execution runtime artifacts: each cycle now persists a latest execution snapshot plus append-only history (`execution-cycles.jsonl`) with a compact operator-facing summary covering regime, confidence, plan action, gating reasons, receipt mode, and alignment policy outcome.
- Added artifact-level tests and revalidated the whole repo with `85 passed`.
- Expanded those execution artifacts to also capture runtime-state, per-route state, and current live-position snapshots, making each cycle record closer to a replayable operational scene.
- Revalidated the whole repo after state-snapshot integration with `86 passed`.
- User clarified a key architecture constraint: the five strategy accounts plus the flat comparison account should keep running continuously; regime/router logic should not be treated as an exclusive account switch for real execution. Instead, review remains the place to compare/interpret performance.
- Added a new pure-simulated `router_composite` virtual account that requires no API keys: it follows router-selected strategy behavior, maintains its own virtual position state, and is now included in execution artifacts so review can later compare it against always-on single-strategy accounts.
- Revalidated the whole repo after router-composite integration with `88 passed`.
- User clarified router/composite switching policy when there is an existing position. If switching with exposure and the target strategy is flat, evaluate whether to keep holding according to priority order: shock/event > crowded extreme > trend > compression > range > chaotic/transition. If the target strategy already has a position, same direction means keep unchanged; opposite direction means close and wait for the next signal rather than immediately reverse.
- Implemented a first reusable `switch_policy` module and wired it into `router_composite`, so composite handoffs now produce explicit `adopt_target_plan` / `keep_current_position` / `close_and_wait` outcomes instead of naive immediate switching.
- Upgraded `router_composite` to read actual target-strategy live position state from the shared live store, so the new same-direction keep / opposite-direction close-wait rules are now grounded in real target account state.
- User clarified the attribution rule: positions belong to the strategy that opened them. Router switching must not rewrite ownership.
- Implemented explicit ownership metadata for `router_composite` positions (`opened_by_strategy` / `position_owner`) and surfaced that attribution into execution artifacts.
- Added a first ownership-aware review/compare snapshot layer (`src/review/compare.py`) and embedded it into execution artifacts, so each cycle now captures router-selected strategy, composite position owner, and per-account state rows for the always-on strategy accounts plus the flat comparison baseline.
- Revalidated the whole repo after compare-snapshot integration with `97 passed`.
- User defined the true periodic review cadence: weekly review every Sunday over the previous Saturday 00:00 UTC -> current Saturday 00:00 UTC; monthly review on the first Sunday over the interval since the last monthly review; quarterly review on the first Sunday every three months.
- User wants market-structure recognition to move toward a learning system, with weekly calibration feedback and monthly strategy-parameter discussion; monthly review is the main parameter-discussion layer.
- Added `src/review/framework.py` plus tests and updated `FRAMEWORK_FINAL.md` to formalize weekly/monthly/quarterly review windows, focus areas, and adjustment boundaries.
- Revalidated the whole repo after review-framework integration with `101 passed`.
- Added `src/review/report.py` plus tests to generate cadence-aware review report scaffolds with standard sections for market summary, account comparison, router-composite review, parameter review, and quarterly structural review.
- Cleaned up the UTC timestamping in the new report scaffold and revalidated the whole repo after report-scaffold integration with `103 passed`.
- Added `src/review/performance.py` plus tests so review reports can now accept normalized per-account performance inputs (PnL, equity, trade count, fees, exposure time) across the five strategy accounts, `flat_compare`, and `router_composite`.
- Revalidated the whole repo after performance-snapshot integration with `105 passed`.
- Added `src/review/aggregator.py` plus tests so the review report can bootstrap coarse metrics from execution history (`execution-cycles.jsonl`), currently focusing on trade counts and exposure-time ratios while leaving canonical PnL/equity/fee ingestion open for the next layer.
- Revalidated the whole repo after aggregator integration with `107 passed`.
- User clarified a session-management preference: if conversation context is getting too large and a new session would help, warn proactively before switching and update the handoff file first so continuation stays smooth.
- User clarified the periodic review system in detail: weekly review every Sunday over the previous Saturday 00:00 UTC -> current Saturday 00:00 UTC; monthly review on the first Sunday over the interval since the previous monthly review; quarterly review on the first Sunday every three months.
- User wants market-structure recognition to evolve toward a learning system with weekly calibration feedback and monthly strategy-parameter discussion; monthly review is the main parameter-discussion layer.
- User further clarified router/composite attribution: positions always belong to the strategy that opened them; router switching must not rewrite ownership.
- Current crypto-trading review stack status at compaction time: compare snapshot, ownership-aware router/composite review, review framework, report scaffold, performance snapshot input layer, and execution-history aggregator are all in place; next natural step is canonical PnL/equity/fee ingestion for review quality.
