# 2026-03-10

- User approved splitting OKX strategies across separate demo accounts: breakout -> current/default account, pullback -> OpenClaw2, meanrev -> OpenClaw3. This should reduce cross-strategy interference in execution and review.
- Implemented multi-account strategy routing across config, client registry, live trader, reconcile, status/snapshot, and review/reset prep/execution. Key commit: `8325e97 Add multi-account strategy routing`.
- User wants detailed progress reporting in chat, including concrete exec/test steps.
- User specified new weekly reset capital rule: after weekly demo reset, use the smallest available capital among the strategy accounts as the common baseline; allocate each account's three symbol buckets from that baseline rather than assuming equal account funding.
- User specified new stale-exit rule: if a close signal appears but the exchange position is already gone, do not block the system; ignore that exit, notify via Discord, and continue waiting for the next entry signal.
- User approved clearing prior trading data because the rules changed, flattening all positions, resetting demo accounts, converting all available assets to USDT, and restarting the trading system under the new rules.
- Flattened the remaining default account positions successfully before reset: BTC short 1.75, SOL long 173.35, ETH short 489.58; then verified account was flat.
- After user reset the demo account, converted all non-USDT balances in the currently configured default account to USDT. Found that OKX demo spot sells require `tdMode=cross` (not `cash`) for this account mode. Successful conversion orders: BTC 0.99999 (`3375960735071272960`), OKB 100 (`3375960737252311040`), ETH 1 (`3375960739433349120`). Final observed free balance afterward was about 87048.61 USDT with no non-USDT balances remaining in that account.
- At the time of the last check, only the default account was actually loaded from runtime config; the additional two demo accounts had not yet been written into the active `.env`/runtime configuration.
- User wants autonomous sequential execution: finish one OKX task, move to the next without waiting, and after core OKX work continue improving weekly review/reporting with special emphasis on parameter calibration.
- User clarified fee analysis preference for OKX reviews: no need to emphasize per-trade fee statistics; core fee lens should be macro/periodic total fees and fee-to-profit ratio, using high fee burden as a signal to reduce trading frequency. Individual fee records can remain only for debugging/audit.
- User further clarified fee handling in review: fee should be a standalone analysis module specifically for trading-frequency adjustment. If fee burden is high, reduce add/entry frequency; if fee burden is low, make no frequency adjustment from fee alone.
- User decided test-mode data should not participate in review decisions and should not be displayed in the dashboard; treat test-mode as execution-chain validation only, excluded from production-facing analytics and UI.
- Continued ops-dashboard rebuild focused on OKX Buckets. Dashboard is now a four-level tab workspace with dynamic bucket tabs labeled by symbol + strategy, real trade table sorting/filtering, Beijing time display, trade row → chart placeholder linkage, outcome/holding/PnL columns, and GitHub backups at `ops-dashboard` commits `f5e301c`, `3d05ab0`, and `7840d8a`.
- User clarified dashboard product shape: read-only status site, no command execution from web UI; four visual tab levels; only Buckets page needs full implementation now; bucket tabs should use symbol + strategy instead of bucket numbers; future requirements include interactive candlestick charts, time+price box selection, and indicator subpanels.
- OKX trading runtime hardened substantially. Added richer entry/exit/reconcile metadata persistence (`okx-trading` commit `9574932`), hardened reset flatten + post-reset validation (`f121d68`), exit verification against live positions (`ca8ae33`), automatic retry of incomplete exits until flat or hard fail (`6cfdb23`), bucket lock after incomplete exits (`eabfc8b`), and entry verification with bucket lock on incomplete entries (`4232619`).
- Confirmed historical reconcile anomalies after the three-account split were concentrated in a short 2026-03-10 14:06–14:46 BJ window, suggesting transition/reset noise rather than ongoing multi-strategy account interference. Latest live checks showed current issues were instead caused by reset/flatten closure gaps.
- Verified that a residual `openclaw2` SOL short existed after local weekly reset; new flatten flow successfully detected and closed it. Durable lesson: all position-changing actions (entry, exit, reset) must be submit + verify closed-loop operations, not submit-only.

- 2026-03-10: OKX naming convention updated: **calibrate** = normal weekly post-flatten bucket reset, should return directly to trade mode; **reset** = development-only destructive data wipe/reset, should return to develop mode.
- User finalized OKX mode semantics: calibrate = weekly operational flatten + bucket reset, then return to trade; reset = destructive development reset, then enter test; test completes back to develop.
- OKX trading system mode set now includes test. Current desired lifecycle: reset -> test -> develop, while review -> calibrate -> trade.
- User wants accounts referred to by strategy names, not generic aliases: Breakout, Pullback, Meanrev.
- Updated OKX credential/account labeling so strategy-account mapping is: breakout/default -> Breakout, pullback/openclaw2 -> Pullback, meanrev/openclaw3 -> Meanrev. Breakout default key was replaced with the new API provided by user.
- User decided all three OKX accounts should be standardized to contract mode with one-way position mode and cross margin assumptions for the system.
- Test mode should use only buffer capital from the first account (Breakout/default), fixed test symbol XRP-USDT-SWAP, and should be stress-oriented (higher intensity/frequency than normal use) to validate local/exchange/bucket/reconcile consistency.
- Diagnosed a major source of prior local-vs-exchange inconsistency: multiple OKX run_daemon.sh instances were running concurrently against the same project. Added single-instance protections (pidfile + conflict checks) and confirmed develop mode became clean after removing the stale manual daemon.
- After wiring the first version of test mode, the first real Breakout/XRP test reached order submission and failed with OKX error code 51000 / "Parameter posSide error", indicating contract/position-mode order parameter mismatch remains to be fixed next.
